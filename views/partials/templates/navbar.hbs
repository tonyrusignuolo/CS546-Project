<nav class="navbar fixed-top navbar-expand-lg" color-on-scroll="100" id="sectionsNav">
	<div class="container">
		<div class="navbar-translate">
			<a class="navbar-brand" href="/">
				<img src="/assets/img/hbLogoNav.png" alt="heartbeat">
			</a>
			{{#if user}}
			Welcome back, {{user.firstName}}!
			{{else}}
			Welcome!
			{{/if}}
			<button class="navbar-toggler" type="button" data-toggle="collapse" aria-expanded="false"
				aria-label="Toggle navigation">
				<span class="sr-only">Toggle navigation</span>
				<span class="navbar-toggler-icon"></span>
				<span class="navbar-toggler-icon"></span>
				<span class="navbar-toggler-icon"></span>
			</button>
		</div>
		<div class="collapse navbar-collapse">
			<ul class="navbar-nav ml-auto">
				<li class="nav-item">
					<a class="nav-link" href="/map">
						<i class="material-icons">map</i>
						Map
						<div class="ripple-container"></div>
					</a>
				</li>

				{{#unless user}}
				<li class="nav-item">
					<a class="nav-link" rel="tooltip" title="" data-placement="bottom" href="/profile/login">
						<i class="material-icons">person</i>
						Login
						<div class="ripple-container"></div>
					</a>
				</li>
				{{else}}
				{{#if user}}
				<li class="dropdown nav-item">
					<a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown">
						{{#if user.photo }}
						<img src="{{user.photo}}" alt="Circle Image" class="rounded-circle nav-img">
						{{else}}
						<img src="/assets/img/account_circle.svg" alt="Circle Image"
							class="img-raised rounded-circle nav-img">
						{{/if}}
					</a>
					<div class="dropdown-menu dropdown-with-icons">
						<a href="/profile" class="dropdown-item">
							<i class="material-icons">account_box</i> My Profile
						</a>
						{{#if user.isAdmin}}
						<a href="/admin/main" class="dropdown-item">
							<i class="material-icons">supervisor_account</i> Admin
						</a>
						{{/if}}
						<a href="profile/logout" class="dropdown-item">
							<i class="material-icons">exit_to_app</i> Sign Out
						</a>
					</div>
				</li>
				{{/if}}
				{{/unless}}
			</ul>
		</div>
	</div>
</nav>